<article class="page-narrow">
  <h1>{{ article.title }}</h1>
  <div class="field-help" style="margin-bottom:24px;">{{ article.author }} &middot; {{ article.published_at | date: '%-d %B %Y' }}</div>
  <div class="rte">{{ article.content }}</div>
</article>
